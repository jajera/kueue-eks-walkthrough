{
  "name": "kueue-eks-walkthrough",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run generate:og-image && astro build",
    "preview": "astro preview",
    "generate:diagram": "node scripts/generate-diagrams.mjs",
    "generate:og-image": "node scripts/generate-og-image.mjs",
    "format": "prettier --write .",
    "lint": "markdownlint-cli2 \"src/**/*.mdx\""
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.2",
    "sharp": "^0.35.3",
    "starlight-base-path": "^0.2.1",
    "starlight-theme-vintage": "^0.1.0"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2",
    "prettier": "^3.5.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
